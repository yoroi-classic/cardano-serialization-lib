name: Package Metadata

on:
  pull_request:
    paths:
      - '.github/workflows/package-metadata.yml'
      - 'package.json'
      - 'README.md'
      - 'scripts/build-helper.js'
      - 'scripts/build-helper.test.js'
      - 'scripts/publish-helper.js'
      - 'scripts/publish-helper.test.js'
      - 'scripts/superseded-fork-check.js'

permissions:
  contents: read

jobs:
  publish-helper:
    name: publish-helper
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Install test dependencies
        run: npm ci --ignore-scripts

      - name: Guard superseded fork metadata
        run: npm run test:superseded-fork

      - name: Test generated package metadata
        run: npm run test:publish-helper
